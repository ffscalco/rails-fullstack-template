name: CI Workflow

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main, epic/** ]

jobs:
  lint:
    uses: ./.github/workflows/lint.yml

  security:
    uses: ./.github/workflows/security.yml

  tests:
    uses: ./.github/workflows/tests.yml
